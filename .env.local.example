# Copy to `.env.local` at the repo root. Never commit real keys.
# Minimal set for the /portfolio desk (playbook Steps 3–8).

HELIUS_API_KEY=
HELIUS_RPC_URL=
# Optional shared secret for POST /api/webhooks/helius (and /api/webhooks/helius-portfolio)
# HELIUS_WEBHOOK_SECRET=

# Optional — improves ticker / history reliability + 24h change fill
# JUPITER_API_KEY=
# BIRDEYE_API_KEY=
# COINGECKO_API_KEY=

# Persist webhook alerts across serverless instances (migration: 20260724_portfolio_alerts.sql)
# NEXT_PUBLIC_SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# Required for AI Coach streaming
ANTHROPIC_API_KEY=

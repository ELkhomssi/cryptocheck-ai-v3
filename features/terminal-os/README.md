# Terminal OS

Route: `/terminalOS` (canonical)

Legacy `/terminal` and `/portfolio` redirect here. Mission Control and AI Coach mount as Terminal OS nav workspaces under `TerminalOsProviders`.

Protected by `app/terminalOS/layout.tsx` → `TerminalOsProviders` (React Query).

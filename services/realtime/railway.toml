[build]
builder = "nixpacks"
buildCommand = "npm run build --prefix ../../packages/signal-contracts && npm install && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/healthz"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
